Overview
========

This consists of a series of simple examples of how to define and run jobs and tasks. All examples 
assume that the username and password are "user" and "pswd" respectively. In order for these 
examples to work, you need to register such a user.

The files starting with df_*.py are dataflow tasks.
The files starting with ev_*.py are event tasks.

